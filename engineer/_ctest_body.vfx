module [ctest]
import %idir%/../3d/render.vfx
import %idir%/../3d/cube.vfx
public

\ Compile-only check: importing the modules verifies they load (undefined words,
\ wordlist bugs, forward refs). No spawn/draw -- those crash during command-line
\ cold and are validated in the GUI instead.
." render3d + cube compiled" cr
