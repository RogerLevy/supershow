module [ctest]
import %idir%/../test/allrange.vfx
public

\ Compile-only: importing allrange (main is NOT auto-called) compiles the whole
\ chain incl. smoothing usage. No spawn/draw.
." allrange compiled" cr
