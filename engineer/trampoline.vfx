module [engineer]

public

: used ( - n ) here dp - ;

private

: ?lfree  ?dup -exit lfree ;

public

: remember  ( variable - )
    dup @ ?lfree  0 0 *lstring over !  dp used rot @ lplace ;

: pristine  ( variable - )
    @ lcount dp swap move   quit ;
