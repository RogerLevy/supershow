public

512 ?constant s/node
32768 ?constant MAX_NODES
variable #nodes

class: %node
    s/node field-space
    is-a %dltree
    is-a %poolable
class;

\ Node pool
%node object free-nodes

: recycle ( node - )
    dup dlremove  dup >pool @ dup if push else 2drop then ;

: copy-node ( node-src node-dest - )
    over %node is? not abort" COPY-NODE: Source is not a %node."
    dup %node is? not abort" COPY-NODE: Dest is not a %node."
    {
        ( obj ) %node -> next-ofs @ +   this dup sizeof   %node -> next-ofs @ /string   move   \ preserve node links
    } 
;

: *node ( class - node )
    free-nodes #items if
        free-nodes pop
        swap over construct
    else
        #nodes @ MAX_NODES = abort" Maximum node limit (32768) exceeded."
        1 #nodes +!
        align dmake 
        free-nodes over >pool !
    then 
;