public

512 ?constant s/node
16384 ?constant MAX_NODES

class: %node
    s/node field-space
    is-a %dltree
    is-a %poolable
class;

\ Node pool

MAX_NODES %node array nodes

%node object free-nodes

[: free-nodes push ;] nodes each
%node >template { free-nodes pool ! }

: recycle ( node - )
    dup dlremove  dup >pool @ push ;
