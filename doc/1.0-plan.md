# VFXLand5 1.0 Planning

Goal: Finalize forge and engineer APIs. Ship supershow as a coherent 2D pixel art game engine. Use lib/ approach for development with snapshots for dormant projects.

## Repository Identity

- "vfxland5" = era/workspace name, not a package
- "supershow" = the actual product (2D pixel art engine)
- forge/engineer = implementation details of supershow, bundled together
- Consider renaming repo to "supershow" to clarify identity
- [x] `%vfxland5%` substitution renamed to `%supershow%` throughout

Don't architect for hypothetical future engines (e.g., 3D):
- forge has fixed-point math baked in - that's fine, it's supershow-flavored
- If a 3D engine ever happens, fork what's needed into a separate repo
- Speculative generalization isn't worth the complexity for a solo project

## Dependency Principle

**Never nest projects with the same submodules** (diamond dependency problem).

Bad:
```
game/
    supershow/        <- copy 1
    addon/
        supershow/    <- copy 2 (conflict, search confusion, wrong-file edits)
```

Solutions:

| Pattern             | How it works                                    |
|---------------------|-------------------------------------------------|
| Bundle together     | tightly-coupled components live in same repo    |
| Source-only layer   | addon has no submodule, assumes engine loaded   |
| Shared lib          | all projects reference same lib/ copy           |
| Top-level owns deps | only game has submodule, addons plug in         |

Any dependency should exist in **exactly one place** in the tree.

## Architecture

```
supershow/            The distributable engine (repo)
    forge/            Foundation (language extensions, Allegro bindings, fixed-point math)
    engineer/         Runtime shell (window, loop, REPL, hot-reload)
    src/              2D game engine (actors, sprites, tilemaps, audio)
    workzone/        GUI toolkit and IDE/REPL ecosystem
```

Principles:
- forge/engineer/workzone = bundled components, versioned together
- supershow = curated collection of modules with defined interfaces
- Modules isolate namespaces; swappable components (e.g., audio) can coexist
- Breaking changes require 2.0

## Workspace Structure

```
vfxland5-work/                      Era/workspace folder (not a repo)
    lib/
        supershow/                  Stable release - all projects reference this
            forge/
            engineer/
            src/
            workzone/
            addons/                 Engine extensions, versioned with supershow
    supershow-dev/
        supershow/                  Active development
            forge/
            engineer/
            src/
            workzone/
            addons/                 Engine extensions, versioned with supershow
        tests/
        experiments/
    games/
        darkblue/                   Uses lib/supershow via %LIB% macro
        kvn/
        ...
```

**Workflow:**
- Daily development: games reference lib/supershow, clean search, one copy
- Ship/archive a project: snapshot lib/supershow into project folder, remove from main workspace
- Return to dormant project:
  - Quick fix: open standalone, don't add to workspace
  - Real update: migrate to latest lib/supershow, bring back into workspace

**WorkZone:** Moves into supershow repo. Tightly coupled, versioned together, both potentially needed at runtime.

**lib/ vs addons/:**

| Folder   | Contains                                         | Can depend on                         |
|----------|--------------------------------------------------|---------------------------------------| 
| lib/     | Self-contained packages, versioned independently | Other lib/ packages (including forge) |
| addons/  | Supershow extensions, versioned with the engine  | lib/ (any supershow layer)            |

Addons live inside the supershow repo under `addons/`. They are not packaged with base supershow but are tracked and versioned together with it. They may freely use engine internals. Once an addon becomes self-contained enough to version independently, it graduates to `vfxland5/lib/`.

Examples:
- `lib/supershow/` - the game engine
- `lib/fixedvec/` - standalone fixed-point vector math (uses forge, but independent package)
- `addons/shapes/` - primitive drawing (circles, rects, lines) built on the pen/color system
- `addons/player2/` - AI NPC integration (extends supershow, future)
- `addons/spunk/` - room-grid world system (extends supershow, future)

The test: **Could this be versioned independently?**
- Yes → vfxland5/lib/
- No, it's an extension of the supershow ecosystem → supershow/addons/

Addons can depend on any supershow layer (forge, engineer, or full engine). A forge-only addon is still an addon if it's extending the ecosystem rather than being a standalone reusable package.

---

## forge

### forge/lib/gamemath.vfx
Status: DONE — moved to src/gamemath.vfx

### forge/lib/dictionary.vfx
Status: DONE — moved to sandbox/dictionary.vfx

### forge/lib/fstools.vfx
Status: DONE
- Rewritten with clean functional API (fread, fwrite, fappend, fdelete, frename, etc.)
- Proper OS error codes via core.vfx file word fixes
- All operations log via .info/.error

### NIBS: validate property accessors (`-> prop` / `>prop`)

`-> prop` and `>prop` should check that the named word is actually a property and
throw if not. Overloading a property name (e.g. a local named `tw` while `%tileset`
has a `tw` property) is fine *as long as you never access it as a property* — but
if you do, `>prop` / `-> prop` silently compute a bogus offset and corrupt memory.
This cost a long debugging session: `tw` shadowed as a local in gs `*new-level`
made `>tw` read a wild offset and scribble on the dictionary, surfacing much later
as a corrupted `move` validation in an unrelated screen. A property-ness check
would turn that into a clean error at the access site.

(Alternative: store properties in a dedicated vocabulary so a shadowing local
can't collide — but that greatly complicates NIBS. Given the nature of NIBS and
Forth, the pragmatic rule stays "don't shadow property names as locals"; the
accessor check is the cheap safety net for when it happens anyway.)

### forge/lib/allegro5/vga13h.vfx
Status: KEEP
- Foundational 8x8 bitmap font
- Used by engineer (default font) and supershow games
- Correct location - low-level graphics primitive

### forge/lib/allegro5/input.vfx
Status: DOCUMENT AS-IS
- Incomplete (no joystick)
- Current functionality is sufficient for 1.0
- Document limitations; joystick can be added later without breaking API

### forge/lib/lstring.vfx
Status: KEEP
- Used by infrastructure (NIBS, trampoline, strout, JSON serialization)
- Not used at game level, which is fine

### forge/lib/strout.vfx
Status: REPLACED by strout2.vfx
- Old strout.vfx removed from forge.vfx include chain
- strout2.vfx implements new API: strout ( xt - ) / str@ ( - a len )
- Dynamic heap buffer, grows automatically, no fixed size limit
- Existing callers (engineer/window.vfx, stage.vfx, spunk editors) need migration

---

## engineer

### engineer/border.vfx
Status: DONE — moved to src/border.vfx

### Other engineer files
- (pending evaluation)

---

## supershow

### WorkZone integration
Status: DONE
- Moved into supershow/workzone/
- Versioned together with supershow

### Identify core vs swappable components

Core (supershow-specific):
- actor.vfx
- stage.vfx
- sprites.vfx
- node.vfx
- traits.vfx
- workzone/ (GUI toolkit, IDE)

Swappable (could coexist with alternatives):
- waveplay.vfx - sample playback; could coexist with synth system

Note: Resolve tilemap/tmcol after other items are settled.

### Modes: decouple from stages, scope to engineer settings

With the World/`%system` layer, the current stage is bound indirectly through `this`
(object → class → origin-module → `mod.stg`), not owned by the mode. So `%mode` is no
longer about stages at all.

---

## cjson

Status: CLEAN UP CJSON2
- cjson is evolving into an integral component (JSON serialization for NIBS)
- cjson2 exists and needs cleanup
- Will become part of supershow distribution

### Planned additions
- Add `j@d` / `j>d` ( - d ) double-cell integer read words (cJSON stores numbers as C double, 53-bit precision covers all 32-bit unsigned values and most 64-bit game data)
- Add `add-long` ( d zkey cjson - ) for writing double-cell integers
- `j@u` can then be expressed as `j@d drop` (workaround for large Tiled GIDs/randomized global IDs coming back wrong via the signed 32-bit valueint field)
- Fix `add-string`/`add-float`/`add-fixed`/`add-int` to route through `add-item` so scalar values can be added to arrays (currently only `add-object`/`add-array` support the array case)
- Rework `serjson>` from a continuation into a plain word taking an xt: `serjson> ( path len xt - )`, using locals instead of module-level values so it is reentrant

---

## spunk

Status: MOVE TO ADDONS
- Simple game world framework (non-scrolling 100x10 room grid)
- Used by some games (kvn, pinky)
- Options: redesign, update after engine finalized, or replace if needed
- Not blocking 1.0

---

## Module system improvements

Status: DONE
- module.vfx replaces scope.vfx
- Per-module public/private wordlists
- Explicit `import` / `reexport` with circular-import detection
- `import:` / `reexport:` batch syntax
- `forth-public` section for words that must be globally visible
- `import-as` for qualified loading of multiple instances of the same file
- `||` works across all section modes
- `import` / `import-as` accept bare names (brackets optional); `reexport` still requires brackets
- `module` doubles as access/experimentation: re-entering an existing module is valid
- `quit` removed from forge.vfx — full forge now loads
- nibs, nibs-tools, nibs-compact, nibs-json → `module [nibs]`
- cjson2 → `module [cjson]`
- oversight, checks, array3, logging, fstools, etc. migrated
- `enough-flag` / `enough` moved to core.vfx as general iteration primitives
- `load-script` deferred in nibs-json (no longer relies on external definition)
- forge/lib/ → lib/; forge/lib/allegro5/ → allegro5/ (top-level)
- allegro5 files → `module [allegro5]`; engineer.vfx imports and reexports it
- engineer files → `module [engineer]`; ls.vfx → `module [ls]`
- dltree.vfx moved from lib/ to forge/, added to forge.vfx after array3.vfx
- stage.vfx: `sprivate` call → `module [name]` evaluate (pending)

To streamline (post-`[supershow.actor]` refactor):
- Streamline module name resolution: `import core`, `import [core]`, and
  `import path/file.vfx` all resolve a module but read like different mechanisms.
  Settle on one canonical form and make the bracket/bare distinction meaningful
  (or drop it). Resolution should be obvious from the call site.
- Make reexport chains discoverable: importing `[supershow]` silently pulls
  core/assets/actor/etc. transitively — hard to tell what you actually get
  without tracing every `reexport`. Want a way to see a module's effective
  exported surface (a diagnostic, e.g. extend `module.`).
- Drop redundant re-imports: a module that imports+reexports `[actor]` already
  gets core/assets through it; re-importing them adds noise without effect.
- Merge import+reexport into a single word. The `import [x] reexport [x]` pair is
  the common case for dependency tiers; collapse it to one word. This reframes the
  two by intent: plain `import` = private dependency (I use it, don't pass it on);
  the merged word = public dependency (I use it AND surface it to my importers) —
  mirroring the public/private section split. `reexport` stays as the primitive
  for surfacing something imported privately earlier.
  - Name candidates (the distinguishing feature is the re-export — words flow
    onward through you — so name for that, not the import half):
    - `expose [x]` — "x's interface is exposed through me"; makes a module read as
      a list of what it deliberately surfaces. (Leading candidate.)
    - `adopt [x]` — "I take x on as my own"; ownership flavor.
    - `provide [x]` — "I provide x to my importers"; accurate, a touch passive.
    - `import+ [x]` — leans on the `+` = "and more" convention; pairs with `import`.
    - (`include` would be ideal semantically but is taken by file inclusion.)

Advanced isolation (future):
- Store entire search order at module creation time
- Each module sees only what it declares as dependencies

---

## fstools detailed analysis

### Inconsistent patterns
- `read` / `_?read` / `?read` / `load-file` - four ways to read files, each with different error handling
- `write` vs `write{`/`data,`/`write}` - two paradigms for writing
- Error handling is a mix of `abort"`, `throw`, `.abort`, and silent catch

### Noise
- `write`, `read`, `load-file` all print to console unconditionally

### Naming
- `-file` and `ext` are cryptic
- `file,` vs `load-file` - inconsistent conventions

### Global state
- `#1` value used by the write{ } words - not reentrant

### Hardcoded assumptions
- `>asset-path` knows about `dat` and `..\` - project-structure-specific, may belong in supershow

### Candidate words to keep (pending review)

| Word          | Purpose                         |
|---------------|---------------------------------|
| load-file     | Read file to allocated memory   |
| write         | Write buffer to file (rename?)  |
| fexists?      | Check file existence            |
| dir-exists?   | Check directory existence       |
| fsize         | Get file size                   |
| ensure-dir    | Create directory                |

### Candidate words to rethink

| Word            | Issue                                           |
|-----------------|-------------------------------------------------|
| >asset-path     | Move to supershow? It's game-project-specific   |
| write{ } family | Decide: keep streaming API or drop it?          |
| read            | Overlaps with load-file                         |
| -file / ext     | Rename to be clearer, or drop                   |
| ?read / _?read  | Clarify purpose or consolidate                  |

---

## Open questions

- [x] How to handle scope.vfx improvements without breaking existing code?
      - Resolved: module.vfx replaces scope.vfx entirely
      - scope.vfx retained as a file but is no longer loaded

---

## Sequence

1.  [x] Set up new workspace structure (lib/supershow, supershow-dev/)
2.  [x] Move WorkZone into supershow repo
3.  [x] Review and clean up fstools
4.  [x] Alias ME as THIS; rename existing THIS usages (mostly WorkZone)
5.  [x] Implement DESTROY in nibs.vfx
6.  [x] Rename nib2.vfx to nibs.vfx (and nib2-tools.vfx, nib2-compact.vfx accordingly)
6b. [-] Refine / complete TODO items in nibs.vfx
      [x] - Implement automatic template resize in >TEMPLATE
      [x] - Make DERIVE parse-ahead (renamed stack form to DERIVE-CLASS)
      [ ] - Fix >prop form not finding properties defined in other modules (-> and implicit access work; >prop does not)
6c. [ ] Post-1.0 NIBS fixes
      [ ] - Add >static syntax
      [ ] - Fix protocol propagation
      [ ] - Fix inheritance of traits that inherit from already-inherited traits
      [ ] - Validate class in MAKE
      [x] - Fix `class:` … `class;` not restoring the current (definitions) wordlist. Across a class block the definitions wordlist changes and isn't restored, so words defined after a class in a module land in a leaked wordlist (invisible to importers — e.g. `/model` was unreachable from another module until a re-`public` was added). Worked around with a re-`public` after the class in `3d/model.vfx`; remove that once fixed. (Candidate culprit: `class:`/`class;`/`property`.)
6c. [x] Refine the compact class syntax
      - Property suffixes: ! (save), :n :a :$ :l$ :p :f :xt (type), :%class (objref)
      - Suffixes can be combined: score!:n  buf! 256  ref:%actor
      - Field space now accepted anywhere before first property
      - .class/.trait show ! after saved properties
7.  [x] Rework logging system
8.  [x] Review and possibly refine the execution context system
      - Moved allocator@ from ec-cell to plain value (no EC involvement)
      - Added with-heap / with-dict scoped allocator words
      - Added in-heap? / in-dict? predicates
      - Added ?alloc (allocator-aware allot+erase)
      - Updated %array and %stack constructors to use ?alloc
      - Added %array :destruct (frees heap data buffer)
9.  [x] Rework strout API
10. [x] Move border.vfx to supershow (with gamemath)
11. [x] Move gamemath to supershow
12. [x] Move dictionary to sandbox
13. [x] Clean up cjson2
14. [x] Design swappable audio interface
15. [x] Tilemap / tmcol (was tilemap4/tmcol4)
16. [x] Rename numbered files and remove old files
      - Deleted: scope.vfx, tmcol3.vfx, forge/main.vfx
      - Renamed: array3→array, strout2→strout, tilemap4→tilemap, cjson2→cjson
17. [-] Document input.vfx limitations — dropped; the only real limitation (no joystick,
          addable later without breaking the API) is already noted in the forge/input.vfx section
18. [x] Rework scope system: proper module system with explicit imports and controlled namespacing
19. [x] Finish module system migration (forge files below quit, engineer)
19b.[x] Rename %vfxland5% substitution to %supershow%
19c.[x] Move dltree.vfx from lib/ to forge/
20. [ ] Adopt dot notation for property access
      - Change property accessors from `>x` to `.x`
      - Rename print words from prefix to postfix dot: `.data` → `data.`, `.name` → `name.`, `.r` → `r.`
21. [ ] Add .info/.warn/.error logging throughout engine and middleware
22. [-] Build isolated tests/demos from old project code
      [x] - asset-autoload test (verify dat/ bitmaps and other asset types load and register correctly and load on startup)
      [x] - actor/sprites demo (adapt "treats" project — pooled actors, per-frame behavior)
      [ ] - "rpg" tiled demo with scrolling and basic collisions (adapt supershow-demos/rpg + sandbox/scrolling)
      [-] - waveplay demo (sample loading, playback, streaming, modify params in realtime)
      [x] - viewport demo (demonstrated in tmscroll - done)
      [ ] - execution context nesting test
      [x] - aabb collision demo
      [x] - shape intersection demo (coldemo.vfx — all 6 cursor shapes vs 5 static shapes)
      [x] - tilemap collision test (tmwalk.vfx — tmslide?, cut-corners, collectible tiles)
23. [ ] Rework oversight
      [ ] - Improve reporting readability (stack traces, validation messages)
24. [ ] Fix slow paste in VFX console when repl-game is active (by temporarily pausing the loop while pasting most likely)
25. [ ] Add Tiled support
26. [x] Add graphics primitives (from workzone)
27. [x] Revamp the execution context system ( add `ECVAR>` form, that pushes old value and restoration routine onto return stack, get rid of E{ E} <AT and ECP field )
28. [x] Fix EMPTY
29. [x] VFX Forth DLL bindings bypassed the module system — `extern` defaulted to `InExternals?`,
          placing foreign-function names in the always-searched EXTERNALS vocabulary regardless of
          module scope. Fixed by setting `InCurrent` as system policy in forge.vfx, so externs honor
          CURRENT and flow through the module system like everything else (reexported only to
          importers). Files using raw `al_*` now `import allegro5`.
30. [-] Write migration guide for 0.x projects — dropped; no standalone guide needed,
          projects migrate ad hoc when actively worked on
31. [ ] Tag 1.0
32. [x] Unify collision response patterns — added response-xt ( col row dir - ) to tmslide; full unification with cgrid not feasible (different collision types), each system has an appropriate callback for its domain
33. [ ] Add rectangle property type (`:rect`) for hitbox-like data (x y w h as integers); standardizes storage and facilitates inspection in `.class` / `.trait` display
34. [x] Integrate mouse support — folded `lib/mouse.vfx` into the new `[allegro5.utils]` module (which un-leaks the allegro5 wrappers from global forth and is reexported engineer → supershow.core → games). Mouse words are now available without a manual import, and `get-mouse` is called automatically each frame in `poll` (go.vfx, after `poll-keyboard` so mouse-button deltas work). It inverse-transforms by the persisted global transform (zoom + fullscreen bezel offset), so mouse comes out in logical app space — the natural common ground. Games wanting world coords can still re-call `get-mouse` under their own camera transform. Workzone migration to the module system remains a separate later task.
35. [ ] Re-implement textmode in Forth — **deferred past 1.0 (out of scope)**
      Replace textmode_renderer.c with a pure-Forth implementation built on the supershow
      tilemap system + a GPU shader. Remove all C source/header/build files for textmode.
      - Tile data encodes attributes in a packed 32-bit cell: `$AAFFBBII`
        - `II` = character index (0-255)
        - `BB` = background color palette index (0-255)
        - `FF` = foreground color palette index (0-255)
        - `AA` = attribute flags (blink, flip H/V, bounce, shake, pulse, etc.)
      - Shader unpacks the cell per-tile, looks up fg/bg from a color palette texture,
        and applies attribute effects (blink = time-based alpha, flip = UV mirror, etc.)
      - Forth API: words to set character, colors, and attributes at (col, row)
      - Goal: eliminate the C dependency entirely; textmode becomes a supershow addon
36. [ ] Investigate FOR/DO loop alternatives that don't use the return stack (>R / I conflicts are a recurring source of subtle bugs)
37. [ ] Improve fonts and text rendering
38. [ ] Cold-load crash: loading (and especially spawning actors from) the full supershow
          stack via a command-line `include` at cold throws `-57005` (Windows access
          violation). It surfaces as VFX's modal exception window with a cascade of unrelated
          RS traces — uncatchable by Forth `catch`, nothing reaches the terminal/log. Loading
          the SAME code after startup/GO (normal GUI flow) works fine. Suspected
          engineer startup-ordering issue (command-line input is processed before
          `frigid`/GO fully bring the runtime up). Impact: blocks headless/automated testing
          of supershow-level code; the compile-only harness works for forge-level libs only.

39. [ ] "Transform pen": Currently 3D models must override `render` purely to skip the 
          2D `x 2@ 2>i at` pen step (pointless under a perspective transform). A 3D pen 
          that defines the active transform would make the 2D and 3D systems consistent.
          (`modelview` conceptually already does this, but it doesn't quite work like the pen)

41. [ ] Clean out old documentation and create new up-to-date documentation
42. [x] Port tween system (Dark Blue has the newest version probably)
43. [ ] Restore the improved "undefined word" error that names the missing word.
          The current `Err# -13 ERR: Undefined word` only shows the source line + caret;
          it doesn't say which word failed to resolve. The old behavior reported the
          actual name (e.g. `%blip?`), which is far easier to diagnose — especially for
          words pulled in via `evaluate` (JSON `"class"`/`"tileset"` names, etc.) where
          the caret points at an `evaluate` call site, not the offending token.

44. [ ] Make the `"..."` z-string literal form (forge/strlit.vfx) work with spaces.
          The `rec-string` recognizer turns `"asdf"` into a z-terminated string literal
          (so keys read as `"asdf"` instead of `z" asdf"`). But recognizers only see one
          whitespace-delimited token, so `"hello world"` is split into `"hello` and
          `world"` and fails to match — only space-free strings work today. Fix: have the
          recognizer consume from the input stream up to the closing `"` (re-parse the
          remainder of the line) rather than operating solely on the single token it's
          handed.

45. [ ] Improve the bitmap and tileset APIs.
          Surfaced while adding tileset hot-reload (gs editor + `reload-tileset`).
          - Sub-bitmap lifecycle is leaky and manual. `update-tileset` re-slices by
            minting fresh `%bitmap` wrapper objects via `wrap-bitmap make` over the same
            `baseid` range, orphaning the previous wrappers (Forth-object leak), and it
            does NOT free the old Allegro sub-bitmaps (caller must, as `-tile-bitmaps`
            now does for reload). Give a tileset clear ownership of its slice range with
            proper teardown (free both the wrappers and the albmps) reused by slice /
            re-slice / unload / reload.
          - Reloading a bitmap from disk has no first-class word. `reload-tileset` open-codes
            `bitmap-file >zpad al_load_bitmap` + swap `id>bmp >albmp !` + destroy old. Want a
            `%bitmap`-level `reload ( bmpid - )` (or `reload-from-file`) that owns this, so
            tilesets and any other consumer build on it.
          - Bitmap field access is clumsy: `bmpid id>bmp >albmp @` to reach the albmp, the
            id↔object↔albmp↔path conversions (`id>albmp` `id>bmp` `albmp>id` `bitmap-file`
            `?bmp>parent`) are a scattered web. Consolidate into a small, consistent accessor set.
          - Decide the embedded-vs-disk source-of-truth policy. Bitmaps embed their data at
            definition time but hot-reload deliberately ignores that and reads the live file;
            make this a documented, deliberate API choice (e.g. a "watch source file" flag)
            rather than incidental behavior.
          - Consider folding the file-watch primitive (`file-mtime`, via Allegro's fs entry)
            into a general engine hot-reload facility so it isn't editor-specific.

46. [ ] Survey and clean out old/legacy engineer words (e.g. `watch`, `info`).
          Inventory the words engineer exposes, flag the stale/superseded/ad-hoc ones
          (leftover dev helpers, things replaced by newer systems, names that collide
          with or shadow better APIs), and decide keep / rename / remove for each.
          Goal is a tighter, intentional engineer vocabulary for 1.0.

47. [ ] Redefine `%mode` as a persistent bundle of *engineer settings*, swapped on mode change:
  `show>` vector, zoom, backdrop color, current font, and similar per-context display state.
    - [ ] Remove the stage binding from the mode layer (mode-stage.vfx); stages follow `this`.
    - [ ] Decide whether `mode` rides the same origin-module indirection as `stg@` (a `mod.mode`
          field + `mode@`) or stays a separate explicit current-mode.

48. [ ] Streamline module import syntax

### Priority
- **6b.** NIBS - High-Priority items
- **48:** Module system: Streamline import resolution syntax; make reexport chains discoverable; merge `import`+`reexport` into `expose` (or equivalent)
- **43.** Restore the improved "undefined word" error (report actual missing name, not just caret)

### Secondary
- **37.** Improve fonts and text rendering
- **45.** Improve the bitmap and tileset APIs
- **47.** Modes: Decouple `%mode` from stages — redefine as bundle of engineer settings; remove mode-stage.vfx binding; decide `mode@` indirection

### Low / Deferred
- **46.** Survey and clean out old/legacy engineer words (`watch`, `info`, etc.)
- **22.** Build isolated tests/demos (rpg tiled, waveplay, viewport, EC nesting)
- **41.** Clean out old documentation and create new up-to-date documentation
- **31.** Tag 1.0
- **20.** Adopt dot notation for property access

### Post-1.0
- **6c.** NIBS - Low-Priority items
- **cjson:** Planned additions (`j@d`/`j>d`, `add-long`, array scalar fix for `add-string` etc., `serjson>` rework)
- **35.** Re-implement textmode in Forth (shader-based, eliminate C files)
- **21.** Add `.info`/`.warn`/`.error` logging throughout, fix logging issues
- **25.** Add Tiled support
- **33.** Add rectangle property type (`:rect`) for hitbox-like data (x y w h as integers); standardizes storage and facilitates inspection in `.class` / `.trait` display
- **23.** Rework oversight
- **24.** Fix slow paste in VFX console when repl-game is active
- **36.** Investigate FOR/DO loop alternatives that don't use the return stack (>R / I conflicts are a recurring source of subtle bugs)
- **38.** Cold-load crash — command-line `include` of the full supershow stack at cold throws -57005 (blocks headless testing)
- **39.** "Transform pen" for 3D consistency (3D pack is experimental, not part of 1.0 API)
- **44.** Make the `"..."` z-string literal form work with strings containing spaces


Note: Old projects stay on their current submodules. Migration happens per-project when actively worked on, not all at once.
