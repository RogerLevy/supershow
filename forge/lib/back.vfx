\ Forge backward compatibility shim

redef aka [[ {
redef aka ]] }

aka >. >p
aka .> >i 
aka 1i pfloor

redef aka wcount lcount
aka wplace lplace
redef aka wappend lappend
aka wslurp lslurp
redef aka w$, l$,
aka w$@+ l$@+ 

aka 2i 2pfloor

aka array[ array{
aka array] array}
aka fast[ fast{
aka fast] fast}
aka data[ temp{
aka data] temp}
aka +m m{
aka -m m}

: $object ( ... class name len - object ) ( - object ) 
    screate dmake ;

: each@       ( ." EACH@ : WARNING: Deprecated." cr )       each ;
aka item-size slot-size
aka max-items max-slots

aka <int :int
aka <fixed :fixed
aka <float :float
aka <cstring :cstring
aka <xt :xt
aka <addr :addr
aka <ref :ref

aka instance dmake